import { Typography, Grid } from '@mui/material';
import React from 'react';

const Home = () => {

    const x = 1;


    return (
        <Grid>
            <Typography>Home</Typography>
        </Grid>
    );
};


export default Home;
